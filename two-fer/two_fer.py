def two_fer(name = None):
     #two = ", one for me."
    # name = one + self + two
     if name is None:
      return "One for you, one for me."
     else:
       self = "One for %s, one for me." % name 
       return self

